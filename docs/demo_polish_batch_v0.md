# 遊べるデモ 磨き込みバッチ — 監査トリアージ＋実装記録 v0（2026-07-13〜14）

Fable監査（`playable_demo_audit_v0.md`）を受け、実機フィードバック17件の実装順を確定して着手した記録。オーナー就寝中の自律実装分（「終わり次第やれるところ全部」）を含む。担当＝4.8単独（ブランチ `claude/manzai-slg-foundation-wprqlu`）。

## オーナー確定（2026-07-13）
- **Q1 次の一手 = 声の最小配線パック優先**＋衝突しない安全枠を並行。
- **Q2 ⑪決勝 = 準決敗退の特別化に置換**。M-1本家型フル決勝演出は棚上げ（1年版は決勝に構造的に届かず＝観客ゼロの画面を膨らませない・監査§1-3/§3-3）。
- **Q3 ①能力UP = 整理だけ**。Fable再設計は発注しない（監査§3-1-2＝この画面の意思決定ループは"核"）。
- ⑰・④は後ろ倒し。⑯の解釈・⑧の額・A-2の審査員別行はオーナー確認待ち。

## 実装済み（このバッチ・全てgolden非干渉・各コミット build SUCCEEDED）
| コミット | 項目 | 内容 |
|---|---|---|
| `490ae53` | 声A-1/A-2 | 心の声プール増補（lowStamina/lowMoney/losing 2→6・passedLines 2→5・平常 3→13）＋予選講評 pass+4/fail+3（無記名◯行）。逐語corpus 0be8a11 |
| `3264549` | 声A-5 | 削除されていた年次独白を復元（躍進/停滞/貧乏を決定的分岐・相方名を含まない◯行）＋「才能がひとつ灯った」の実体なき常時表示を除去（監査§1-4-①） |
| `27a8ff8` | ⑫⑬ | 道中大会結果の「頂グランプリ」固定バッジをGP系のみに是正＋通過/敗退を単発語(入賞/敗退)へ（`calendar.tournament(inWeek:)`で型判別・判定不変） |
| `a11041b` | ⑩ | 遠征ボタンに実額（¥10,000/¥30,000・Calendar既存値） |
| `e1551b4` | ⑥⑦ | 金額/体力ピル 9.5→12・体力ゲージをグラデ/グロー/数値強調 |
| `ab39e57` | ①② | ρ=0の休眠チップ(freeChip)/説明(explainer)を非表示＋つぎの本番の「じぶんN/要求N(=通過ライン)」数値を撤去（朱の要求ライン・「+N」は残す） |
| `5432aad` | ⑮手応え | 押下の沈み PressableStyle 0.97/-6%→0.95/-10%（⑮の段階的結果テキストは別途＝下記） |
| `aeb145b` | ⑤ | カード実行に scale1.06 のポップを重畳（既存0.12s引き抜きに乗せる・間合い予算不変） |

## 後ろ倒し／オーナー判断待ち（着手前に一言ほしい）
- **⑮ 段階的結果テキスト＋reaction配線**（※押下の手応え強化は `5432aad` で実施済み・残るは段階テキスト部）：押す→一拍→結果の"間"はコアループのフロー変更＝実機の手応えをオーナーが見て決めたい。reaction辞書（現在は呼び出しゼロの死蔵）＋corpus §1-7/§8 は用意済み。配線先＝WeekMainView の行動実行部（`WeekMainView:357` variantCard 近傍）。
- **⑪→準決特別化**：1年版の"実質最終戦"は敗者復活の敗北（ライン76）で、専用corpus行が無い（`会話.準決敗退`は復活戦が続く前提の3セリフ）。どの敗北を山にするか＝語りの設計判断。年末独白(A-5)を復元したので当面の"締め"は成立。
- **⑯ 寂しい**：対象未確認（週メイン中央の空白 or 仮シルエット）。声配線で最頻画面の言葉は増えたので、まず実機で再評価推奨。
- **⑧ フリーライブ エントリー代**：金額未定＝バランス。規律A（tools→GameCore→gen_golden→swift test）で1コミット。額の提案 or 確認が要る。
- **④ 経験値の桁拡大**：CLAUDE.md B-5（体感は数値でなく演出/言葉で先に）＋目視待ち。声/演出の後に再判断。
- **⑰ 選択肢イベント**：監査=初回プレイテストのシグナル後。UIコード新規＝別タスク（`HANDOFF_choice-events`/`0024`）。
- **A-2 審査員別講評行（§8）**：署名ローテと口調がミスマッチしうる（白波の絶叫が天堂寺名で出る等）＝`review()`の戻り値に話者を載せる話者連動を入れるか要相談。今回は無記名◯行のみ配線。
- **監査§1-4 の残**：通知許諾の無機能／資金不足時の無言落ち／アイテムタイル「準備中」／初回シード424242固定＝除去 or 実装の product 判断。

## 検証
- 各コミット前に `xcodebuild`（iPhone 17 Pro sim）で **BUILD SUCCEEDED** を確認。木は常にビルド可能。
- `swift test` はバッチ開始時 **32件green**（GameCore/tools/乱数消費順に一切触れていない＝golden不変）。
- **全改善を含む最新ビルド**（9コミット分）を iPhone 17 Pro / iPhone 17 の両シミュレータにインストール済み＝**実機プレイテスト待ち**（`HANDOFF_voice-to-real-device.md` Part B の8観点。「寒い」行はそのまま報告で差し替え候補に）。

## 次アクション（オーナー起床後）
1. 実機で声パックを目視（週メインの心の声・予選講評・年末独白）。「寒い」行を報告。
2. 上記「判断待ち」に一言（特に⑮のフロー変更可否・⑧額・⑯対象）。
3. golden枠（⑧/④）に入る時は規律Aで。
