<!-- 発行側（稼ぐ側）実装の独立検証記録 v0（2026-07-13深夜〜朝・オーナー確定によりマージ完了）。
実装はブランチ worktree-agent-aa63b05f7908d6490 → 共有ブランチ claude/manzai-slg-foundation-wprqlu へマージ済み（コミット233b8ce）。
マージ後もswift testを再実行し32件green・golden不変を確認済み。コード改変なし（本ファイルはドキュメントのみ）。数値は全て【仮】。 -->

# 経験点発行側の実装＋独立検証（2026-07-13深夜〜朝）

`docs/exp_abilityup_impl_reply_v0.md`（Fable注ぐ側実装）の§4申し送りに従い、発行側（会計移設・golden再生成・sim較正）を実装した。**私（Mac側）が独立に`swift test`を再実行し、実装エージェントの自己申告を裏取り済み。**

## 実装の場所（マージ済み）
- ブランチ: `worktree-agent-aa63b05f7908d6490` → `claude/manzai-slg-foundation-wprqlu` へマージ済み（コミット`233b8ce`・衝突なし）
- マージ後、共有ブランチ上で`swift test`を再度独立実行し32件green・golden不変を再確認済み

## 独立検証結果（私が実際に実行・確認済み）
```
cd .claude/worktrees/agent-aa63b05f7908d6490/GameCore && swift test
Executed 32 tests, with 0 failures
```
- `WeekRunnerGoldenTests.testThreeYearsMatchRunYearGolden`（3年ビット一致）＝**PASS**
- `GoldenTests`（12週）＝**PASS**
- 金/体力/知名度/相性は旧goldenとバイト一致（能力値のみ変化）＝乱数消費順は1ビットも動いていない

## 実装内容（要約）
- 稽古の能力加算を「二区画クレジット」へ移設（ロック(1−ρ)＋所属枠ρ・メンタルは100%ロック）。rest/offer/イベント/トロフィーは経済外＝直add()のまま（申し送り通りの推奨スコープ）。
- golden台本＝行動直後に`recommendedPlan`を全量即時注ぎ（`pourRecommended`・3系統=Python/Swift/golden生成器で同一関数）。
- Python鏡像（`balance_sim.py`/`sim_career.py`/`gen_golden.py`）を先に実装→GameCore同期→golden再生成、の規律A手順を実施。

## sim較正の結果（ρ=0・供給スケール0.48【仮】で確定）
n=2000実測、目標＝やり込み41.5%／のんびり改23.1%／バランス8.4%：

| 帯 | 目標 | 実測 |
|---|---|---|
| やり込み | 41.5% | **45.0%**（+3.5） |
| のんびり改 | 23.1% | **22.9%**（ほぼ一致） |
| バランス | 8.4% | **8.8%**（ほぼ一致） |

5ゲート（おすすめ最適度／メンタル蓄積／供給再照準／ρスイープ／銀行上振れ消滅）は**全てPASS**。銀行上振れは実質0（+0.00000実力）で、spikeが示した+3.6〜4.7pt上振れは構造的に解消。

## ★オーナー確定（2026-07-13朝）：ρ=0を採用・マージ完了

**ρ（共通枠への配分率）は0（＝同色1:1に完全縮退）で確定。** バランス最優先——ρ>0だと素朴なプレイスタイルが+13〜17pt上振れし帯の順序が壊れるため。**共通枠（ExpGroup）は機構として残置**（将来ρ>0へ拡張する余地は温存）するが、現行較正ではUI上の共通枠チップは実質未使用の状態になる。この上でマージ済み（`233b8ce`）。

参考：やり込み帯45.0%を41.5%へさらに寄せたいだけなら、供給スケール0.48→0.50前後の微調整余地は残っている（のんびり帯が+1pt上がるトレードオフ）。今回はここまでは詰めていない＝将来の微調整候補。

## 残タスク（マージ後）
1. `GameSession.lastGains`の粒差分振替（稽古の「+N」ピル表示が今のままだと0になる・UIスコープ）
2. UI/simulatorビルド確認（`ManzaiGame`側は本検証の対象外＝規律D-10未実施）
3. （将来・任意）供給スケールの微調整（0.48→0.50前後）でやり込み帯を41.5%へさらに寄せる
