# Fable開始プロンプトv2（ラストイヤーの奇跡＝主・角の同時成立裁定＝副／1セッション2問・コピペ即用）

> 使い方: Claudeのセッションで `/model claude-fable-5` に切替え、下の三連バッククォート内をそのまま最初のメッセージに貼る（冒頭に /model 行を含む・切替済みなら1行目は無視してよい）。
>
> v1との差分: v1はQ1（ラストイヤー）1問だけだった。v2は**受け皿の実験台が3台とも実在し、アンカー検証まで済んでいる**（`exp_lastyear.py`／`exp_corner.py`／`exp_talent_ability.py`・下記「受け皿の現状」）。よってFableは**「台を作る」のではなく「回して裁く」に全振り**でき、これまでblockedだった本命=**Q2「角の同時成立裁定」**も同一セッションで裁ける。v1の禁止事項・合格条件・golden非干渉・出力の形の骨格はそのまま踏襲し、Q2を副問として足した版。
>
> 受け皿の現状（4.8/CLIが構築・検証済・全て【仮】）:
> - `tools/exp_lastyear.py`：Q1用。最終年 `s._yg−X` レバー＋**停滞層(9年目終了ever_final==False)の最終年“単体”分離集計**＋10年目残予算診断まで実装。アンカー3点一致（のんびり22.9%/0.38%・分散41.4%/2.62%・G3 X=0=62.1%）。
> - `tools/exp_corner.py`：Q2用。30pt×SSR相方(cap24)×裏天井×得意A×絆 の5レバー積み上げスキャン。アンカー一致・角セル実測済（分散角=優勝60.75%/到達99.0%）。
> - `tools/exp_talent_ability.py`：得意A(方式A・稽古効率+20%・演技系4能力限定)の単体台。mental/compat/stamina/fameへの漏れゼロを実測確認済。exp_cornerが内部で流用。
> - **golden非干渉は3台とも構造保証＋実測確認済**（`gen_golden.py` は `balance_sim`/`sim_career` しかimportしない・実行後グローバル完全復元）。

````
```
/model claude-fable-5

あなたは claude-fable-5。iOS/SwiftUIの漫才師コンビ育成SLG「manzai-game」(GameCoreは純Swift分離・本編は結成10年で完結・数値は全て【仮】)で、残り少ない高性能枠を「構造的バランス／sim較正の“判断”」だけに使う一発勝負のセッションです。実装・量産・doc整合・機械的スイープは別担当(4.8/CLIとMac)がやります。

■このセッションの立て付け（v1から変わった最重要点・最初に飲み込む）
実験台はもう作ってある。あなたは台を書かなくてよい。`tools/exp_lastyear.py`（Q1用）・`tools/exp_corner.py`（Q2用）・`tools/exp_talent_ability.py`（得意A単体）が実在し、3台とも「トロフィー0pt・裏天井/得意OFF」のベースラインが canonical §2 に一致することを実測で確認済み（アンカー継承済＝空回りしない）。golden非干渉も構造保証＋実測で確認済み。だからあなたの全時間は「回して・読んで・裁く」に注げる。**台を作り直すな。回して裁け。** 既存台の集計を足す/セルを増やすのは可（experiment-only・run_yearには触れない）。

今回はQ1（ラストイヤーの奇跡＝主）とQ2（角の同時成立裁定＝副）の2問を1セッションで裁く。Q1に重心、Q2は主にexp_cornerの既測を読んで裁定を言語化する。どちらも合格条件は「スキャン表を出すこと」ではない——表は4.8/CLIでも書ける。あなたに温存された価値は(1)なぜその構造になるのかの診断、(2)緩和/合成が“角の複利”や“二重救済”へ漏れないかの裁定、の言語化だけにある。回さずに机上で結論を書くのは失敗、回すだけで裁定を言語化しないのも失敗。

■最初に必ず読む（この順で・本文はここに委譲）
1. `docs/fable_readiness_v0.md`（今回新設）… 受け皿3台の実在/検証状況・何が【仮】か・Fable後にMacがやること。ここが今回の“地図”。
2. `docs/red_team_v0.md` §1「ラストイヤーの奇跡が数学的に死んでいる」＋§4トリアージ P1… Q1がなぜ温存されているかの一次ソース。
3. `docs/fable_findings_v0.md` Q5「積み上がりの俯瞰＝最大の構造的リスク＝角の複利」… Q2の一次ソース（角スコープ限定補正・SSR角<90%規範）。
4. `docs/pity_calibration_v0.md` §6（周回進行の3段階表）… “壊してはいけない不変帯”の一次数値。①0pt初回帯・③30ptトロコン帯はここが正本。
5. `docs/owner_decision_brief_v0.md` 決定記録①〜⑦/A2… オーナー確定＝蒸し返し禁止の一次ソース。
6. 成長機構の正典（読むだけ・触らない）: `tools/sim_career.py:120-121`（CAP_CURVE=(6.0,0.4,2.0)／CAREER_BUDGET=True）・`:355-361`（run_yearが年頭に累計予算を算出し `B.YEAR_GROWTH_CAP` を上書き・`s._yg`はリセットしない）。奇跡が死ぬ現場。

■環境の前提（勘違いすると空回りする）
- この環境に Swift は無い＝`swift test`は打てず、golden(bit一致)の確定はできない。あなたの成果は「裁定＋simログ」。確定フリップはMacへ引き継ぐ。
- 触るな（golden破壊＝Mac領分・期限内に検証不能）: `sim_career.run_year`本体・`gen_golden.py`・`balance_sim.py`の`add`/成長中核・`CAP_CURVE`定数本体・`canon_v2`の数値。canonicalソースは1行も編集しない。
- 回せる（experiment-only・golden非干渉）: Pythonの実験台だけ。既存の `exp_lastyear.py`/`exp_corner.py`/`exp_talent_ability.py` を回す。集計拡張やセル追加はよいが、機構（レバーの数式）は変えない・全レバーは「一時上書き→finally完全復元」のまま。

■効くレバーの正体（既に台に実装済・仕様として頭に入れる）
- ラストイヤー（Q1・唯一正しいレバー）: 自前年ループで最終年(year==YEARS==10)のrun_year呼び出し直前に `s._yg` を X下げ、伸びしろを+X付与する。`add` の予算は `YEAR_GROWTH_CAP − s._yg`（balance_sim.py:126）。run_yearが `B.YEAR_GROWTH_CAP` を再計算しても `s._yg` は温存（sim_career.py:359-362で非リセット）＝+Xがその年“だけ”生きる。`s._yg` を下げる操作は draw を1回も消費しない＝乱数消費順不変＝golden非干渉。罠（踏むな・台は既に回避済）: (1)run_year前に `B.YEAR_GROWTH_CAP` を代入しても年頭で消える (2)CAP_CURVE基準+Xは累計で全10年に乗り約+10Xになり最終年だけにならない。
- 角の5レバー（Q2・exp_corner実装済）: ①トロフィー30pt=`canon_v2.apply(30)`（CAP_CURVEを+0.02/pt lift・EVENTS_ON=True）②SSR相方=`B.COMPAT_CAP=24`（既定20・相性上限+4）③裏天井=TH3/FLOOR8/BP0.30/MODE C（準決以下のみBURST_P上書き・決勝本番は素・初到達ever_finalで恒久解除）④得意A=`exp_talent_ability.apply(1.20)`（稽古効率+20%・演技系4能力sense/idea/expr/chara限定・mental/compat/stamina/fameには絶対かからない・generous版で角の上限をbracket）⑤絆=compat_start底上げ（BOND_COMPAT_START=8【仮】・最も機構が薄い＝最も仮）。全て一時上書き→finally完全復元。

■既に回っている実測（あなたの出発点・全て【仮】・4.8/CLIがexp_*で先行測定・あなたは再測で確証しつつ“裁定”を言語化する）
【Q1 ラストイヤー・n=800】
- 停滞層はプレイ型で非対称。**のんびり改は奇跡が起きない**：X=0〜4で停滞層(n=593)最終年“単体”到達15.5%/優勝0.34%が完全に不動。**分散稽古型は明確に効く**：停滞層(n=506)最終年 到達30.8%→64.2%・優勝2.17%→13.24%（X=0→4で単調上昇）。
- 原因＝10年目残予算診断：のんびり改は残予算+5.78を“使い残し”（稽古量が少なく予算制約に当たっていない）→伸びしろを足しても無意味。分散型は+2.40とほぼ使い切り→+Xがそのまま成長に化ける。「残予算≈0」は分散family(2.4/3.0)で概ね成立・のんびりで不成立。
- 漏れの所在＝G3（30ptやり込み優勝）が X=0の62.1%基準→X=+4で75.5%（+13pt）跳ねる。主因は**このレバーが「year10到達コンビ全員」に無条件適用**され、停滞層(43/800)でなく“決勝経験済・未優勝”の厚いテール層まで+Xで優勝するため。**停滞層(ever_final==False)ゲート付きにすればG3漏れはほぼ消せる**（設計上の示唆・要検証）。
【Q2 角・n=800/600】
- 積み上げ（分散稽古型）：baseline 41.4%/2.62% → +30pt 89.0%/27.25% → +SSR 97.9%/60.88% → +裏天井 99.1%/61.25% → +得意A 96.9%/58.88% → +絆(=角/全部ON) 99.0%/60.75%。**火力はほぼ全て 30pt×SSR に集中し、そこから先は~60%で飽和（プラトー）**。角−(30pt+SSR)≈−0.12＝完全飽和。乗算的暴走は観測されない。
- 角セル（全部ON・やり込み）：分散 優勝60.75%/到達99.0%（n=600でも60.0%/99.0%＝安定）。のんびり改 優勝7.00%/到達76.5%。**規範判定：角優勝60.75% < 90% → 必勝化せず**（同時成立しても確実には勝てない）。
- 得意A単体は分散に効かない（稽古効率+20%が予算クランプに丸ごと吸われる＝角の中で冗長）。絆はプレイ型と上限力学に完全依存（compat稽古する分散では上限24に飲まれ寄与≒0・compat稽古しないのんびりでは決勝まで生存し+4.62）＝最も仮。

■誤読の訂正（最重要）
「測定は4.8/CLIの仕事だから自分は回さない」は誤り。台は用意されているが、**回して自分の目で確証し、その実測を根拠に予算トポロジ・二重救済・角の飽和を裁くこと自体が、あなたに温存された構造判断の中身**。上の既測を鵜呑みで机上結論を書くのも失敗（再測で確証せよ）。遠慮なく回してください。既測と食い違ったら食い違いこそ報告価値。

■禁止事項
1. golden／`run_year`本体／`gen_golden`／`balance_sim`成長中核／`CAP_CURVE`定数本体／`canon_v2`数値を触らない・`swift test`前提の結論を書かない（experiment-onlyの一時ラップ＋finally復元のみ）。
2. オーナー確定①〜⑦/A2を蒸し返さない: 王者編廃止(⑥)／本編10年(⑦)／裏天井=入れる(BP=30%確定・決勝除外・初到達で恒久解除)／優勝演出=役柄化／点差=M-1全表示／得意=方式A(演技系4能力限定)／メンタル・体力型=MVPは演出のみ／夜行列車GP除外。「10年より延ばすか」「王者編を戻すか」の再議論は禁止。**王者編廃止＝“王者特権で恒久成長解除”を、最終年緩和や角の口実に流用してはならない**（別物）。
3. 恒久能力配布は禁止。Q1緩和は「最終年だけ成長ヘッドルームを+X一時開放」であって能力を後周回へ持ち越すことではない。1問を別設計（メンタル救済・裏天井再設計・新config乱立）に膨らませない。
4. `canonical_v2_spec`の正典数値を勝手に確定フリップしない。提案は必ず【仮】＋simログ付きで`docs/`に書き、確定はMac(golden同期)へ引き継ぐ。
5. **Q2の得意A/裏天井は実験的近似＝“仮の仮”**（Macが run_year へ本実装するまで正典ではない）。角裁定はこの近似の上でしか下せない＝「本実装後に4.8が正典ハーネスで再測する前提」を裁定に必ず明記（=撃ち切らない・方向だけ確定）。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■Q1（主・重心80%）＝ラストイヤーの奇跡の較正
狙い（良いゲーム観点）: 本編10年の最終年に「決勝で勝てないまま来た停滞コンビ」へ“最後の一花”を数学的に蘇らせる。ただし確実な必勝ボタンにはしない——最終年“単体”の到達/優勝が“意味ある幅で”上がればよく、確実に勝てるようになってはいけない。「もう後がない、でもまだ伸びる」の感情のピークを実パワーに乗せることが目的で、優勝を配ることは目的ではない。

合格条件（探索“前”に、数値許容上限まで登録してから回す・全て【仮】・後付け禁止）。以下の許容±を回す前に確定して宣言し、探索後に基準を緩めない:
- 不変G1（0pt初回・のんびり優勝）: 基準 ≈0.62%。許容上限 ＿＿%（例: +0.15pt以内）。
- 不変G2（0pt初回・やり込み優勝）: 基準 ≈3.50%。許容上限 ＿＿%（例: +0.5pt以内）。
- 不変G3（30ptトロコン・やり込み優勝）: 基準 ≈62.1%。許容上限 ＿＿%（例: +1.0pt以内＝“集めた強コンビの複利”に漏れない証明）。**既測ではゲート無しでX=+4に+13pt漏れる＝ここが最大の設計判断点**。
- 蘇生G4: 停滞層（9年目終了ever_final==False）の最終年“単体”到達/優勝が、意味ある幅（下限を数値で先に置く）で上がる。
- 選択則: G1〜G3を割らずにG4が最も立つXを採る。両立しないなら、どの帯を犠牲にするかを明示して裁定。

思考の型（この順で結論を出す）:
1. 診断ファースト: `exp_lastyear.py` を回し、緩和OFFで「10年目残成長予算」を実測（のんびり+5.78使い残し/分散+2.40使い切りを自分で確認）。奇跡が死ぬ度合いを数字で確定。ここを外すと緩和が的を外す。
2. 停滞層の分離: 全体平均では奇跡は埋もれる。台は既に「ever_final==Falseで絞った最終年“単体”の到達/優勝」を分離集計する（st_n/st_reached/st_won）。この分離が判断の肝。
3. 用量反応: X=0→+4 で振り、各Xで(a)停滞層最終年の蘇り(b)不変帯G1/G2/G3の漏れ を同一ハーネスで実測表に（台・シード・n付き）。
4. **G3漏れの機構裁定（この問いの肝）**: 既測どおり“無条件適用”だとG3が跳ねる。**停滞層(ever_final==False)ゲート付きレバー**にすればG3漏れが消えるかを実測で示し、「蘇生は停滞層に限定発動すべき」を機構として裁定する。ゲート無し/ゲート有りの両方を測って弁別せよ。
5. 二重救済チェック: 裏天井ON × 最終年+X の合成セルを測り、「立たせる(裏天井=準決以下ハマ)」と「伸ばす(最終年=実力の最終加速)」が別レイヤーとして加算的に効くだけで決勝の“条件付き優勝率”を跳ねさせないことを確認。跳ねるならどのXで漏れ始めるかを特定。
6. 必勝ボタン化の回避を機構で言語化: 「+Xしても最終年単体で優勝が確実にはならない（一花であって花束でない）」を据え置く力学として言葉に（実力ライン下の停滞コンビは伸びても決勝の素ハマ10%の夜を越えにくい、等）。**のんびり改では奇跡が構造的に起きない（残予算使い残し）という非対称を、バグでなく“予算を使い切った者にだけ最後の伸びが来る”設計思想として肯定するか、のんびり型にも別の出口を用意すべきかを裁定**（＝Q1の隠れた核心）。

■Q2（副・重心20%）＝角の同時成立裁定
狙い: 30pt×SSR相方×裏天井×得意A×絆 の右上角（設計本命ゾーン）が本編10年で同時充足しても必勝化しないか。既測（exp_corner）を再測で確証し、裁定を言語化する。

角の合格規範（`fable_findings` Q5準拠・全て【仮】）:
- (i) 0pt初回帯が不変（のんびり≈23.1%/0.3%・やり込み≈41.5%/2.7%）。→既測でアンカー一致済・再確認。
- (ii) **角優勝 < 90%**（同時成立しても確実には勝てない＝SSR角でも必勝化しない）。→既測60.75%でクリア・確証せよ。
- (iii) 合成が**加算的（弱い超加算まで許容）で乗算的暴走をしない**＝どこかでプラトーに達する。→既測は30pt×SSRで~60%飽和・確証せよ。

思考の型:
1. exp_corner を回し、積み上げスキャン（単独→合成・marginalΔ優勝）と角セルを再測。既測（30pt×SSRに火力集中→~60%飽和・角60.75%<90%）を確証。
2. 加算/乗算の裁定: 「30pt×SSRの弱い超加算 → ~60%でプラトー」「後段3レバー(裏天井/得意A/絆)は予算・相性上限が張り付き加算ゼロ」を機構で言語化。得意A単体が予算クランプに吸われ角内で冗長なこと、絆がプレイ型と上限力学に完全依存で最も仮なことを明示。
3. **“仮の仮”の明示**: 得意A（ラッパで演技系×1.20）と裏天井（BURST_P一時上書き）は実験的近似。角裁定は「この近似の上での方向確定」であり、Mac本実装後に4.8が正典ハーネスで角セルを再測する前提を裁定に刻む。合格規範(ii)(iii)を割る兆しがあるXやレバーがあれば特定。
4. 角スコープ補正の要否: 既測で必勝化しない（60.75%<90%）なら「今は角スコープ補正（合計効率キャップ等）を機構化する必要はない・ただしMac本実装後の再測が条件」と裁定。必勝化の兆しがあれば、`fable_findings` Q5の(a)合計効率キャップ/(b)SSR角犠牲 のどちらを推すか方向を出す。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■出力の形（結論は2枚に【仮】で・pity_calibration/fable_findingsの型を踏襲）
【Q1 → `docs/lastyear_calibration_v0.md`】
1. 推奨: 緩和機構＋緩和幅X＋対象母集団（停滞層ゲートの要否を明記）＋発動条件（全て【仮】）。または“撃たない”提案＋根拠。両立しない場合の犠牲帯の裁定。
2. 根拠の実測: 回した台＝`exp_lastyear.py`・シード・n・用量反応表（OFF→+X・ゲート有無で弁別）・停滞層条件付き表・**裏天井ON×最終年+Xの合成セル**（どのXで漏れ始めるか）。
3. 機構的説明＋確信度と残リスク: なぜ不変帯を壊さないかの据え置く力学（特にG3漏れをゲートで消す機構・のんびり非対称の思想的裁定）。
4. Fable後のTODO（Mac＝run_yearへ最終年緩和＋停滞層ゲートを本実装・乱数消費順不変必須／4.8＝正典ハーネスで再測しcanonical/red_team §1確定／オーナー＝X体感ダイヤルと発動年の非明示演出）。

【Q2 → `docs/corner_arbitration_v0.md`（新設）】
1. 裁定: 角は必勝化するか/しないか（規範ii/iii判定）。角スコープ補正の要否と方向。全て【仮】。
2. 根拠の実測: `exp_corner.py`・シード・n・積み上げスキャン表・角セル・marginalΔ。
3. **“仮の仮”の明示と再測条件**: 得意A/裏天井が実験的近似であること・Mac本実装後に4.8が正典ハーネスで角セルを再測する前提。
4. Fable後のTODO（4.8＝得意A/裏天井のrun_year本実装後に角セル再測・canonical更新／cli-mac＝角セルを固定シード群として標準スキャンに常設＝回帰）。

時間配分はQ1に全振り（8割）、Q2はexp_corner既測の確証＋裁定言語化（2割）。時間が尽きても、Q1診断（残予算＋停滞層分離）＋「G1〜G3を割らずに停滞層最終年を蘇らせるX候補1本（停滞層ゲート付き）」を実測付きで出せれば成功。Q2は最低でも「角60.75%<90%＝必勝化せず・ただし本実装後の再測前提」の1裁定を残す。残りは箇条書きの申し送りに。
```
````
